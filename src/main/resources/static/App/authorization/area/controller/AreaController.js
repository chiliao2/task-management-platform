Ext.define('App.authorization.area.controller.AreaController', {
            extend : 'Ext.app.ViewController',
            alias : 'controller.area_controller',
            init : function(view)
            {
                this.callParent();
            }
        });
